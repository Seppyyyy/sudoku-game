import React, { useEffect } from "react";

export default function GenBoard({ prefilledcells }) {
    function MakeBoard() {
    }

    return (
        <div>
            {/* Content of GenBoard */}
        </div>
    );
}
